name: Test

on: [push,workflow_dispatch]

permissions:
  contents: write
  pull-requests: write

jobs:
  # --------------------------------
  # 1️⃣ Build & test the environment
  # --------------------------------
  build-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11","3.12","3.13"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Micromamba (with caching)
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-file: environment.yml
          cache-downloads: true
          micromamba-version: "latest"
          create-args: python=${{ matrix.python-version }}
      - name: Set git identity for testing
        run: |
          git config --global user.email "runner@example.com"
          git config --global user.name "CI Runner"

      # CESM input data (ESMF meshes) used by the mapping integration tests.
      # ~28 MB, fetched once and reused; the key changes only when the file
      # manifest in tests/utils.py does.
      - name: Cache CESM input data
        uses: actions/cache@v4
        with:
          path: ~/.cache/mom6_forge/inputdata
          key: cesm-inputdata-${{ hashFiles('tests/utils.py') }}
          restore-keys: |
            cesm-inputdata-

      - name: Run pytest
        shell: bash -l {0}
        run: |
          pytest -v --disable-warnings --cov=mom6_forge --cov-branch --cov-report=xml
      - name: Upload coverage report to CodeCov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}


